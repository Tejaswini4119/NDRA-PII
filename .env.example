# NDRA runtime mode
ENV=dev

# API runtime paths
UPLOAD_DIR=./uploads
ARTIFACTS_DIR=./artifacts

# Optional audit file override (used in container)
AUDIT_LOG_FILE=./audit/audit.log

# Optional integration keys
OPENAI_API_KEY=
GEMINI_API_KEY=
